function scrollContact() {
    const section = document.getElementById('scrollkontak');
    section.scrollIntoView({ behavior: 'smooth' }); // Ini akan melakukan scroll ke bagian tersebut secara smooth
}

function scrollTestimoni() {
    const section = document.getElementById('scrollkesini');
    section.scrollIntoView({ behavior: 'smooth' }); // Ini akan melakukan scroll ke bagian tersebut secara smooth
}
